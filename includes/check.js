function checkSearch (code) {
    switch(code) {
        case 100:
            showAlert('Done','','success');
            break;
        case 101:
            showAlert('Failed','','error');
            break;
    }
}